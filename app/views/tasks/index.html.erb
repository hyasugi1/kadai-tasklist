<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |tl| %>
    <li><%= link_to tl.id, task_path(tl) %> : <%= tl.content %> &gt; <%= tl.status %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの登録', new_task_path %>
